@model IEnumerable<Domain.Entities.Product>

@{
    ViewBag.Title = "Админ панель: МЕНЮ";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="panel panel-default">
    <div class="panel-heading">
        <h3>МЕНЮ</h3>
    </div>
    <div class="panel-body">
        <table class="table table-striped table-condensed table-bordered">
            <tr>
                <th class="text-right">ID</th>
                <th>Название</th>
                <th class="text-right">Цена</th>
                <th class="text-center">Действия</th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-right">@item.Prod_Id</td>
                    <td>@Html.ActionLink(item.Prod_Name, "Edit", new { item.Prod_Id })</td>
                    <td class="text-right">@item.Prod_Price.ToString("# грн.")</td>
                    <td class="text-center">
                        @using (Html.BeginForm("Delete", "Admin"))
                        {
                            @Html.Hidden("Prod_Id", item.Prod_Id)
                            <input type="submit"
                                   class="btn btn-default btn-xs"
                                   value="Удалить" />
                        }
                    </td>
                </tr>
            }
        </table>
    </div>
    <div class="panel-footer">
        @Html.ActionLink("Добавить блюдо", "Create", null,
            new { @class = "btn btn-default" })
    </div>
</div>
